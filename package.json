{
  "name": "com-boombox-projecty",
  "version": "1.0.10",
  "description": "fridaContainer written in TypeScript and JavaScript",
  "private": false,
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build:esbuild": "npm exec esbuild -- --bundle --format=esm --outfile=../com.rookie.v/api/index.js --watch index.ts",
    "prepare": "npm run build",
    "build": "frida-compile index.ts -o index.js",
    "watch": "frida-compile index.ts -o index.js -w",
    "dmlog": "frida-compile ./utils/dmlog.ts -o ./utils/dmlog.js"
  },
  "dependencies": {
    "frida-il2cpp-bridge": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "esbuild": "^0.25.2",
    "frida-compile": "^16.4.2",
    "@types/frida-gum": "^18.8.2"
  }
}

